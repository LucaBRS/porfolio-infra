# Copy this file to .env and fill in the values
# Never commit .env

# App
ENV=production

# CORS — comma-separated list of allowed origins
ALLOWED_ORIGINS=https://lucabarsottini.dev,http://localhost:3000

# GCP — future BigQuery / service account credentials
GCP_PROJECT_ID=
GCP_SERVICE_ACCOUNT_KEY_PATH=
